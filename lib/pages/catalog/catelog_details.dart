import 'package:flutter/material.dart';

class CatelogDetails extends StatelessWidget {
  const CatelogDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('welcomem'),
      ),
    );
  }
}
