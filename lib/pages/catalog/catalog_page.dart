import 'package:flutter/material.dart';

class CatalogoryPage extends StatelessWidget {
  const CatalogoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Catalog')),
    );
  }
}
